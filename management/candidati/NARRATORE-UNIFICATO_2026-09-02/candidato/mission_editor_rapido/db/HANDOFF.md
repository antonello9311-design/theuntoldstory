# MISSION-RAPID-DB-RESUME-002 · handoff candidato DB

TASK-ID · `MISSION-RAPID-AGG5-028` (integra `MISSION-RAPID-AGG4-026`, recovery `018`, schema `019`)

Scope toccato · Candidato DB dell’editor rapido in `INSTALL.sql`, contratto in `CONTRACT.json` e recovery conservativa in `RECOVERY.sql`. Nessuna applicazione in produzione eseguita.

Contratti usati/modificati · Baseline Mission Creation post «Adatta missione» (`mission_creation_preflight_v2` MD5 `2bfa56c88e45c52eb4495ee1f7c5d870`, `mission_create_complete_v1` MD5 `8a512021ec0335f6eddb8b859607b056`); PNG Builder compile MD5 `13909ce28a3a4b32fd8b6fbd0a5c944a`; board open MD5 `7efaab206f6b8f1fab92b72c848d5d72`; schema esterno `mission-rapid-draft/1`; output compiler `mission-authoring-output/1`.

Decisioni prese / OPEN · Il Builder vivo certifica nuovi PNG solo come piani privati `planned` e non li rende utilizzabili da Mission Generic. L’MVP risolve quindi un bundle `adopted` già approvato come autorità meccanica e tecnica; identità, personalità, comportamento, conoscenze e limiti restano overlay editoriale approvato e congelato nel documento missione. Il catalogo e l'anteprima richiedono anche il binding meccanico attivo/approvato e la versione meccanica approvata, dalla quale espongono statistiche, `vita_max`, `chakra_max` e sole abilità supportate. Le correzioni editoriali vengono rivalidate e diventano la compiled corrente; source e mappa specifica restano canoniche e pinndate alla spec server. Le condizioni non standard sono valutate dal solo consumer privato su round, attori, oggetti e movimenti nativi; l'endpoint JSON è chiuso. L'hook su `progress_step` richiede l'esatta definizione viva, non opera sulle missioni senza regole rapide e ha recovery anti-drift sulla definizione salvata. Il publish è atomico e idempotente: preflight, creazione/sigillo/selezione piano, condizioni, recovery e apertura board. OPEN: controverifica indipendente, rilascio riservato, configurazione della policy Edge e collaudo nella Staff Test Room.

Prove eseguite e risultato · Check task guard pulito; composizione con «Adatta missioni» fissata sulla definizione viva MD5 `0bc40e0ff963c26a63e218f2aa95dd94`. Suite aggregate 153/153 PASS, zero chiamate provider. Il primo apply review8 è fallito atomicamente al parser PostgreSQL sui due confronti `IS DISTINCT FROM CASE`; schema rapido e hook sono rimasti assenti. Entrambi i CASE sono ora parentesizzati e la forma è stata accettata dal parser vivo con un blocco `DO` senza persistenza. Il test contrattuale impedisce la regressione. Sul SQL: unica transazione, delimitatori bilanciati, nessun `DELETE`/`DROP`/`TRUNCATE`, RLS e grant espliciti, policy authoring inizialmente spenta.

Rischi o regressioni da verificare · La sintassi PostgreSQL e la composizione del hook vanno controverificate prima dell’apply. Il budget della bozza deve coincidere con le policy runtime vive; una differenza blocca il sigillo. Le condizioni terminali vanno provate nel percorso reale della Staff Test Room e devono chiudere incontro/transizione una sola volta senza effetti sulle risorse reali. Il profilo autonomo di base resta quello Ninja Book approvato: l’overlay editoriale non crea una nuova identità Ninja Book globale.

Passaggio richiesto al PM · Congelare il candidato per review; se verde, distribuire DB ed Edge nel perimetro riservato e collaudare esclusivamente nella Staff Test Room protetta, con `testperfunzioni` e `Riuji`, verificando risorse e sessioni prima/dopo. Nessun Docker.
