# Workdir isolato del rilascio Esame. Nessun link al progetto principale.
# Il progetto destinatario deve essere sempre specificato con --project-ref.
# Entrypoint relativo a questo config, secondo la documentazione Supabase.
project_id = "tus-exam-narrative-release"

[functions.exam_genin_ai]
enabled = true
verify_jwt = false
entrypoint = "../edge/index.ts"
